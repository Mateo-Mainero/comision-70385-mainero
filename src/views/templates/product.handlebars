<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{prod.title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-4">
        <div class="bg-white p-6 rounded-lg shadow-lg">
            <h1 class="text-3xl font-bold mb-4">{{prod.title}}</h1>
            <p class="text-gray-700 mb-4">{{prod.description}}</p>
            <p class="text-gray-900 font-bold mb-4">Precio: ${{prod.price}}</p>
            <p class="text-gray-600 mb-4">Stock disponible: {{prod.stock}}</p>
            <a href="/api/products" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Volver a Productos</a>
        </div>
    </div>
</body>
</html>